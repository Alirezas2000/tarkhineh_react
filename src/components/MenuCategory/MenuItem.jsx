/*const MenuItem = ({ img, title, isFood = false }) => (
  <div className="cart">
    <a className="cart--link" href="#">
      <div className={`cart--image ${isFood ? 'food' : ''}`}>
        <img 
          src={require(`../../assets/images/PNG/${img}`)} 
          alt={title} 
        />
      </div>
      <div className="cart--title">
        <h4>{title}</h4>
      </div>
    </a>
  </div>
)

export default */